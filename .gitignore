# 의존성·빌드 결과
node_modules/
dist/
build/

# 환경 변수·비밀 (예시 템플릿만 커밋)
.env
.env.*
!.env.example

# Supabase 브라우저용 로컬 설정 (example 복사 후 사용, 커밋 금지)
scripts/supabase-config.js

# 보안·자격증명 (키·인증서·토큰 파일 등)
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
*.der
*.jks
*.keystore
*.mobileprovision
secrets/
credentials.json
*-credentials.json
*.secret
.netrc

# 로컬 도구 상태 (bkit 플러그인 캐시·세션)
.bkit/

# Claude Code 로컬 설정 (PC마다 다름)
.claude/settings.local.json

# Vercel CLI 로컬 링크 정보
.vercel

# OS·에디터 잡파일
.DS_Store
Thumbs.db
*.log
