# 의존성·빌드 결과
node_modules/
dist/
build/

# 환경 변수·비밀 (예시 템플릿만 커밋)
.env
.env.*
!.env.example

# 로컬 도구 상태 (bkit 플러그인 캐시·세션)
.bkit/

# Claude Code 로컬 설정 (PC마다 다름)
.claude/settings.local.json

# Vercel CLI 로컬 링크 정보
.vercel

# OS·에디터 잡파일
.DS_Store
Thumbs.db
*.log
